name: ci

on:
  push:
    branches:
      - "main"

jobs:
  build:
    strategy:
      matrix:
        app-name: ["apple", "banana"]
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Echo Start
        run: echo "Start"
      -
        name: Matrix
        uses: ./.github/actions/matrix
        with:
          app-name: ${{ matrix.app-name }}
